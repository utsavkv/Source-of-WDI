# Interactive component

Hello world!
```{r eval = FALSE, include = FALSE}

# select a subset of countries and compare their GDP per Capita over different years
library(dplyr)
write.csv(df.long %>%
            filter(Year == 2020 &
                     Indicator.Code == 'NY.GDP.PCAP.CD') %>%
              select(Country.Name, Value) %>% na.omit() %>%
              slice_sample(n = 100) %>%
              mutate(Value = Value/10),
          'd3_data/interactive_data.csv', 
          row.names = FALSE)
```

<iframe src="interactive.html" width="700" height="500"></iframe>